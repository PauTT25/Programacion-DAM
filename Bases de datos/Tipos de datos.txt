Tipos de datos

INT = Numero entero
VARCHAR = Cadena de texto
TEXT = texto largo
DATE = Fecha

CREATE TABLE clientes(
   dni VARCHAR(9),
   nombre VARCHAR(50),
   apellidos VARCHAR(255),
   email VARCHAR(100)
);
